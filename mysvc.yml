apiVersion: v1
kind: Service
metadata:
  name: mynginx-svc
  labels:
    app: myonlineapp
spec:
  type: NodePort
  ports:
  - port: 8080
    nodePort: 30002
    protocol: TCP
  selector:
      app: myonlinestore
